# Frontend env. Copy to .env to override defaults.

# Base URL of the backend API. Required when frontend and backend run on
# different hosts (production) — locally it defaults to http://localhost:3002.
VITE_API_URL=http://localhost:3002
